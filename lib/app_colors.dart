import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF000000);
  static const Color background = Color(0xFFFFFFFF);

  // Text Colors - Updated for better contrast
  static const Color textPrimary = Color(0xFF000000);    // Black for primary text
  static const Color textSecondary = Color(0xFF424242);  // Dark gray for secondary text

  // UI Colors
  static const Color grey = Color(0xFF9E9E9E);          // Medium gray
  static const Color lightGrey = Color(0xFFE0E0E0);     // Light gray for borders
  static const Color white = Color(0xFFFFFFFF);         // White

  // Status colors
  static const Color success = Color(0xFF4CAF50);       // Green
  static const Color error = Color(0xFFf44336);         // Red
  static const Color warning = Color(0xFFFF9800);       // Orange

  // Additional colors
  static const Color lightBackground = Color(0xFFF5F5F5); // Light background variant

  // Gradient colors for image placeholders
  static const Color gradientStart = Color(0xFF1565C0);
  static const Color gradientEnd = Color(0xFF1976D2);
}